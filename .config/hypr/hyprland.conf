# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

autogenerated = 0 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=DP-1,1920x1080@144,0x0,1
monitor=HDMI-A-1,1080x1920@60,1920x0,1,transform,3


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
env = XCURSOR_SIZE,24
env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = WLR_NO_HARDWARE_CURSORS,1
#env = GTK_THEME,Dracula

#Execute Once
exec-once=waybar
exec-once=export QT_QPA_PLATFORMTHEME=qt5ct
exec-once=swaybg -o DP-1 -i ~/Pictures/wall.jpg -m fill
exec-once=swaybg -o HDMI-A-1 -i ~/Pictures/vert-wall.png -m fill
exec-once=dunst
exec-once=wl-clip-persist --clipboard both
exec-once=swayidle -w


# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 20
    border_size = 2
	col.active_border=0xff5e81ac
    col.inactive_border=0x66333333
    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 12
    blur = yes
    blur_size = 6.8
    blur_passes = 2
    blur_new_optimizations = on

    drop_shadow = yes
    shadow_range = 5
	col.shadow=0xffa7caff
    col.shadow_inactive=0x50000000
}

#Blurs
blurls=waybar
blurls=lockscreen

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic-mouse-v1 {
    sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

# Window rules for window - workspace bindings
windowrulev2 = workspace 2,class:^(Vivaldi-stable)$
windowrulev2 = workspace 3,class:^(thunar)$
windowrulev2 = workspace 4,class:^(Code)$
windowrulev2 = workspace 5,class: ^(org.telegram.desktop)$
windowrulev2 = workspace 6,class: ^(Spotify)$
windowrulev2 = opacity 0.95 override 0.9 override,class:^(Code)$ # set opacity to 1.0 active and 0.5 inactive for Code
windowrulev2 = opacity 0.9 override 0.80 override,class:^(Vivaldi-stable)$
windowrulev2 = opacity 0.9 override 0.80 override,class:^(thunar)$



# default workspace bindings
workspace = DP-1, 2
workspace = HDMI-A-1, 1


# workspace monitor bindings
wsbind=1,HDMI-A-1
wsbind=4,HDMI-A-1
wsbind=5,HDMI-A-1
wsbind=2,DP-1
wsbind=3,DP-1
wsbind=6,DP-1



# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, kitty
bind = $mainMod, Q, killactive, 
bind = $mainMod, F, fullscreen, 
bind = $mainMod, M, exit, 
bind = $mainMod, X, exec,wlogout 
bind = $mainMod, V, togglefloating, 
bind = $mainMod, D, exec, rofi -modi run,drun -show drun
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod SHIFT, F, exec, vivaldi-stable
bind = $mainMod SHIFT, T, exec, thunar
bind = $mainMod SHIFT, V, exec, code
bind = $mainMod SHIFT, S, exec, spotify
bind = $mainMod SHIFT, M, exec, telegram-desktop
bind = $mainMod SHIFT, C, exec, grimshot --notify save area
# swaylock-effects from AUR
bind = $mainMod SHIFT, L, exec, swaylock --screenshots --clock --indicator --indicator-radius 100 --indicator-thickness 7 --effect-blur 7x5 --effect-vignette 0.7:0.7 --ring-color 1b3e5a --key-hl-color 101d2f --line-color 00000000 --inside-color 00000088 --separator-color 00000000 --grace 2 --fade-in 0.2

# Move workspaces relatively within monitors
bind = $mainMod SHIFT, left, workspace, m-1
bind = $mainMod SHIFT, right, workspace, m+1

# Move workspaces across monitors
bind = $mainMod SHIFT, up, workspace, e+1
bind = $mainMod SHIFT, down, workspace, e-1


# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
